# Screenshots

This folder contains sanitized captures used for release documentation.

Included captures:

- `claude-refresh-banner.png` - Claude connector panel showing 12 tools and the refresh banner
- `claude-permissions.png` - Claude connector panel showing the read-only and write/delete tool groups

Sanitization notes:

- captures were cropped to the connector content panel
- no email, token, or private shell content is included
- the visible connector URL is public

If additional captures are needed later, sanitize them before adding them here.

Files:

- [`claude-refresh-banner.png`](claude-refresh-banner.png)
- [`claude-permissions.png`](claude-permissions.png)
